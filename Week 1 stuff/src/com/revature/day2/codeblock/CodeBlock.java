package com.revature.day2.codeblock;

public class CodeBlock {

	//instance code block that executes before constructor
	//when object is instantiated. Note that we can't reference
	//the object usng "this" since the object isn't instantiated yet
	
	{...}
	
	//static code blocks are similar, these execute one, when the class is loaded in JVM
	static {...}
	
	/*
	 * These code blocks give use a window upon the creation of these objects. 
	 * We can use these code blocks to inject behavior when we instantiate objects
	 */
	
	
	
	
	
}
